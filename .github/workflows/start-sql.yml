name: Start SQL Instance

on:
  schedule:
    - cron: '0 11 * * *'  # Ejecuta a las 11:00 UTC (8:00 AM Argentina)
  workflow_dispatch:

jobs:
  start-instance:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
    - name: Auth
      uses: google-github-actions/auth@v2
      with:
        workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
        service_account: ${{ secrets.SERVICE_ACCOUNT_EMAIL }}

    - name: Set up gcloud
      uses: google-github-actions/setup-gcloud@v2
      with:
        project_id: ${{ secrets.GCP_PROJECT_ID }}
        install_components: 'beta'

    - name: Turn SQL instance ON
      run: |
        gcloud sql instances patch mi-instancia-sql --activation-policy=ALWAYS
